{
  "name": "@iarx/api",
  "version": "0.1.0",
  "private": true,
  "description": "API de domínio da IARX — NestJS sobre PostgreSQL com RLS",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "node --test dist-test/**/*.test.js"
  },
  "dependencies": {
    "@iarx/contracts": "0.1.0",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1"
  }
}
