node_modules/
dist/
# O CSS de tokens é versionado: o gate de CI compara o gerado com o commitado.
!packages/tokens/dist/
!packages/tokens/dist/tokens.css
build/
.next/
coverage/
*.log

# Segredos — nunca versionados. A chave service_role do Supabase ignora RLS
# (ver docs/anexos/H-supabase.md, H.2) e não pode sair do ambiente de CI.
.env
.env.*
!.env.example

# Supabase CLI
supabase/.temp/
supabase/.branches/

.DS_Store

# Saída de compilação da API e dos contratos
apps/api/dist/
apps/api/dist-test/
packages/contracts/dist/

# Saída do Playwright — artefato de execução, não fonte.
test-results/
playwright-report/
